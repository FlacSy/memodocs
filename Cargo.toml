[package]
name = "memodocs"
version = "1.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
pyo3 = { version = "0.18", features = ["extension-module"] }


[dev-dependencies]
criterion = "0.4"

[lib]
name = "memodocs"
crate-type = ["cdylib"]